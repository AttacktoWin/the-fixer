[gd_resource type="ShaderMaterial" load_steps=2 format=2]

[sub_resource type="Shader" id=22]
code = "shader_type  canvas_item;

uniform vec2 target = vec2(0.5);
uniform float radius = 0.5;
uniform float a;
uniform float b;

void fragment(){
	COLOR = texture(TEXTURE,UV);
	vec4 screen_color = texture(SCREEN_TEXTURE,SCREEN_UV);
	float dist = abs(distance(target,SCREEN_UV));
	float strenth = mix(0,1,1.0-smoothstep(dist,a,b));
	if(dist<=radius && COLOR.a>0.0){
		COLOR.a = strenth;
	}
}
"

[resource]
shader = SubResource( 22 )
shader_param/target = Vector2( 0.5, 0.5 )
shader_param/radius = 0.203
shader_param/a = 0.22
shader_param/b = 0.186
